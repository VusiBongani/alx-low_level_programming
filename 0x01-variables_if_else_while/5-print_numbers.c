#include <stdio.h>

/**
 * main - entry point
 * description: printing numbers that are between 0 and 9 included
 * Return: always 0 (Success)
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	return (0);
}
