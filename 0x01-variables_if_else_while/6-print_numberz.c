#include <stdio.h>

/**
 * main - Entry
 * description: printing numbers less than 10 using SCICode
 * Return: always 0 (success)
 */

int main(void)
{
	int n;
	int p;

	for (p = 0; p < 10; p++)
	{
		putchar (48 + n);
		n++;
	}

	putchar('\n');
	return (0);
}
